{
  "name": "cdk-practice",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@aws-cdk/aws-amplify-alpha": "^2.91.0-alpha.0",
    "@hookform/error-message": "^2.0.1",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "amazon-cognito-identity-js": "^6.3.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-hook-form": "^7.45.2",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build ",
    "build-css": "npx tailwindcss build src/pages/Home.css -o public/Home.css",
    "deploy": "npm install && npm run build && cd infra && npm install && npm run cdk-bootstrap && npm run cdk-synth && npm run cdk-deploy",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "tailwindcss": "^3.3.3"
  }
}
